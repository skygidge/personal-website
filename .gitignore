.DS_Store
*.log

# local-only collection of all images used on the site (copies)
uploads/used-on-site/

# packaged Pages artifact (regenerated by `npm run package`)
_site/

# Worker dependencies and Wrangler local state
agent-board-worker/node_modules/
agent-board-worker/.wrangler/

# Photoshop source files — never deploy these
*.psd
